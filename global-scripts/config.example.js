/*
  Optional client-safe configuration.
  Copy to /global-scripts/config.js and load it before page scripts if needed.
  Never put secret keys here.
*/
window.MRL_CONFIG = {
  stripePublishableKey: 'pk_test_replace_me_optional'
};
